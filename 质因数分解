/*我们用上面的试除法以及Eratosthenes 筛法，找到 [2，x \sqrt{x} 
x]中x的所有的因子，并且把该因子在x中全部除尽，使得x中没有了该因子的参与。
比如：12 = 2 ∗ 2 ∗ 3 12=2*2*312=2∗2∗3，那么，对于12而言，我们就需要把两次2都除尽，使得12中只剩下了3的参与。
*/
那么，显然，在上述的过程中可以整除x的数字都是质数。

void primes(int k,int x)
{
	for(int i=2;i<=sqrt(x);++i)
	{
		if(x%i==0)
			while(x%i==0)
				x=x/i, c[i]++;
	}
	if(x!=1)	c[x]++;
}
//c[i]记录了质因数出现的次数
